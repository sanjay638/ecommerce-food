python manage.py runserver --nothreading --noreload

After running of the above line in your command prompt you get any errors then you need to install the following code:

1. pip install django
2. pip install pandas
3. pip install matplotlib
4. pip install sklearn

After installing the above code then run again the first line code on this document.
